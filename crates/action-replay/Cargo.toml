[package]
name = "action-replay"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Record + replay browser action sequences for browser-use-rs"

[dependencies]
browser-core = { path = "../browser-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
