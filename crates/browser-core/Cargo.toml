[package]
name = "browser-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Browser trait + backends for browser-use-rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
async-trait = "0.1"
