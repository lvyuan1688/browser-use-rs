[package]
name = "vision-mod"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Vision module for browser-use-rs — screenshot diff + similarity"

[dependencies]
image = "0.25"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
