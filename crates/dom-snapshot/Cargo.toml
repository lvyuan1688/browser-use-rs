[package]
name = "dom-snapshot"
version = "0.1.8"
edition = "2021"
license = "MIT"
description = "Structured DOM snapshot for browser-use-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
